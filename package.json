{
  "name": "grupo-gazin",
  "author": "kelvin diego da silva soares < kelvin.fabiana@gmail.com >",
  "private": true,
  "scripts": {
    "web:dev": "yarn lerna  run --stream web:start",
    "web:build": "yarn lerna run --stream web:build",
    "web:test" : "yarn lerna run --stream web:test",
    "backend:dev": "yarn lerna  run --stream back:start",
    "backend:build": "yarn lerna run --stream back:start:dev",
    "backend:test" : "yarn lerna run --stream back:test"
  },
  "devDependencies": {
    "lerna": "^4.0.0",
    "cross-env": "^7.0.3",
    "fork-ts-checker-webpack-plugin": "^6.3.2",
    "tsconfig-paths-webpack-plugin": "^3.5.1"
  }
}
